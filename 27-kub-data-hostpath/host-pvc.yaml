# 영구 볼륨 claim
apiVersion: v1 
kind: PersistentVolumeClaim
metadata:
  name: host-pvc
spec:
  volumeName: host-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources: # capacity에 대한 대응이다. 요청 용량
    requests: 
      storage: 1Gi
